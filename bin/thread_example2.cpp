#include <iostream>
#include <cmath>
#include <fstream>
#include <algorithm>
#include <vector>
#include <cassert>
#include <cstring>
using namespace std;

#include <boost/thread.hpp>
#include <threadpool.hpp>

int main() {

  if (argc <= 1){
    std::cout << "Usage: " << argv[0] << " inputArg" << std::endl;
    exit(1);
  }

  return 0;
}
