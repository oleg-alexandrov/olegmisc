#!/usr/bin/perl
use strict;        # insist that all variables be declared
use diagnostics;   # expand the cryptic warnings

MAIN:{

  my $len = scalar(@ARGV);
  if ($len < 1){
    print "Usage: $0 files\n";
    exit(0);
  }

  
  
}
